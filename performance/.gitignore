# Generated captures (per-run stats + environment provenance), now platform x vendor labelled
# e.g. rlgl_windows_nvidia/, rlvk_linux_amd/
rlgl_*/
rlsw_*/
rlvk_*/

# Reports ARE committed (labelled report_<backend>_<label>.html + report_comparison_<label>.html)

# Run logs
*.log

# Built tools (extensionless on Linux/macOS)
src/*.exe
src/*.o
src/performance_capture
src/performance_report

# rlmtl (native Metal) captures follow the same rule
rlmtl_*/
